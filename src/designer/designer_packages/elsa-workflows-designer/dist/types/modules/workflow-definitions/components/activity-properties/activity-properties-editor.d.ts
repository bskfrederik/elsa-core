import { EventEmitter } from '../../../../stencil-public-runtime';
import { Activity, Variable } from '../../../../models';
import { ActivityUpdatedArgs, DeleteActivityRequestedArgs } from "../../models/ui";
import { OutputDefinition } from "../../models/entities";
export declare class ActivityPropertiesEditor {
  private slideOverPanel;
  private renderContext;
  private readonly inputDriverRegistry;
  private readonly eventBus;
  constructor();
  workflowDefinitionId: string;
  activity?: Activity;
  variables: Array<Variable>;
  outputs: Array<OutputDefinition>;
  isReadonly: boolean;
  activityUpdated: EventEmitter<ActivityUpdatedArgs>;
  deleteActivityRequested: EventEmitter<DeleteActivityRequestedArgs>;
  private selectedTabIndex;
  show(): Promise<void>;
  hide(): Promise<void>;
  componentWillRender(): Promise<void>;
  render(): any;
  private getSelectedTabIndex;
  private createTabs;
  private createInputs;
  private findActivityDescriptor;
  private onSelectedTabIndexChanged;
  private onActivityIdChanged;
  private onActivityDisplayTextChanged;
  private onCanStartWorkflowChanged;
  private onRunAsynchronouslyChanged;
  private onInputPropertyEditorChanged;
  private onOutputPropertyEditorChanged;
  private updateActivity;
  private renderCommonTab;
  private renderInputTab;
  private renderOutputTab;
  private renderTaskTab;
}
