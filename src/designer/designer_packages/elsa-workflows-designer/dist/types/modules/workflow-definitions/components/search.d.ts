import { FunctionalComponent } from "../../../stencil-public-runtime";
export interface SearchProps {
  onSearch: (term: string) => void;
}
export declare const Search: FunctionalComponent<SearchProps>;
