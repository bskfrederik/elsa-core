import 'reflect-metadata';
import { ActivityDisplayContext, ActivityDriver } from '../../services';
export declare class DefaultActivityDriver implements ActivityDriver {
  display(context: ActivityDisplayContext): any;
}
